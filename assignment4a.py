#create a function that is able to calculate the area of a rectangle
def area():
    length = int(input("Enter the first number: "))
    width = int(input("Enter the first number: "))
    area = length * width
    print("The area is:", area)
    
area()